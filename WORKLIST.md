# WORKLIST — 遊戲機制還原盤點

> **目標(使用者指示 2026-08-07):遊戲機制要還原成原版。** 本檔逐子系統盤點「原版有什麼、
> 我們做到哪」,並標明每一項的**一手證據**。階段順序見 `PLAN.md`,規則見 `CLAUDE.md`。
> 最後更新:2026-08-08

## 0. 盤點方法(為什麼可以相信這份清單)

不是憑對 U5 的印象列的,而是從原版檔案本身推出來的三類證據:

1. **24 個 `.OVL` 的檔名與大小** —— 原版把每個子系統切成一個 overlay,檔名直接說明它負責什麼,
   大小則是該子系統的程式碼量(16-bit 機器碼),可當工作量指標。
2. **`DATA.OVL` 的 43 個明文表** —— 裝備、怪物、店名、法術、狀態、選單、劇情關鍵字全在裡面。
3. **資料檔本身** —— `KARMA.DAT`(業報)、`QUESTION.DAT`(創角)、`*.PTH`(NPC 路徑)、
   `*.CBT`(戰鬥地圖)…每個檔案的存在就是一項機制的存在證明。

⚠ 標「待確認」的項目,是**只知道它存在、還不知道規則細節**的 —— 那些要等 P3 讀反編譯碼
或手冊 OCR,**在有證據前不要照自己對 U5 的印象實作**。

## 1. 原版子系統總覽(依 OVL)

**overlay 機制已解**(`docs/re/59`):`ULTIMA.EXE` 0x817B 有 25 筆模組名表(索引 = overlay 編號),後面緊接 **164 個各 12 B 的進入點樁**(`call far` loader → 編號 → `jmp far`)。`DATA.OVL` 的進入點數是 **0**,獨立佐證它是純資料。幾組 overlay **共用同一個交換槽**(`TOWN`/`MAINOUT`、`FLAMES`/`NPC`/`COMBAT`/`DNGLOOK`、`CAST2`/`COMSUBS`/`FONT`)—— 那正是`COMBAT` 與 `COMSUBS`、`SHOPPES` 三段要拆檔的原因。重跑:`tools/dev.sh python3 tools/ovl_thunks.py gamedata`。

| OVL | 大小 | 子系統 | 狀態 |
|---|---|---|---|
| `DATA.OVL` | 48,464 | 資料表(非程式碼) | 🔶 已解出世界地圖索引、詞典、43 個明文表 |
| `SJOG.OVL` | 8,800 | **J)immy + O)pen + G)et**(名字是指令字母縮寫)| 🔶 **用途已定位**(`docs/re/59`):17 個進入點,全 overlay 最多。四個信物編號 0xB4–0xB7 只有它同時比對(= `sub_154BC` 的簽名),上鎖門 0xB9/0xBB 與力場 0x97/0x98 也只有它齊備。⚠ **S 未證** —— Search 最專一的 0x4E 在它裡面一次都沒出現 |
| `CAST.OVL` | 8,560 | 施法(主) | ⬜ |
| `INTRO.OVL` | 8,400 | 開場動畫與主選單 | ⬜ |
| `DUNGEON.OVL` | 8,016 | 地牢(第一人稱) | ⬜ |
| `CMDS.OVL` | 7,440 | 指令處理(玩家輸入的動詞) | ⬜ |
| `COMBAT.OVL` | 7,408 | 戰鬥 | ⬜ |
| `MAINOUT.OVL` | 7,344 | 地表主迴圈 | 🔶 地圖顯示與移動已有,規則未做 |
| `TOWN.OVL` | 6,256 | 城鎮場景 | ⬜ |
| `SHOPPES.OVL` | 5,936 | 商店(主) | ⬜ |
| `COMSUBS.OVL` | 5,216 | 戰鬥副程式 | ⬜ |
| `DNGLOOK.OVL` | 5,040 | 地牢視角繪製 | ⬜ |
| `NPC.OVL` | 4,912 | NPC 行為與排程 | ⬜ |
| `TALK.OVL` | 4,880 | 對話系統 | ✅ 解碼 + 流程 + **中譯覆蓋層**(`docs/re/06`、`docs/re/35` §6);譯文 **1712/1713(99.9%)**,重算:`u5dump talkwork` → `tools/audittalk.py` |
| `ZSTATS.OVL` | 4,880 | 角色數值畫面(Ztats) | ⬜ |
| `LOOKOBJ.OVL` | 4,560 | 觀察物件 | ⬜ |
| `CAST2.OVL` | 4,544 | 施法(續) | ⬜ |
| `FONT.OVL` | 3,744 | 字型繪製 | ✅ 自己的實作(倚天 CJK + 原版 8×8) |
| `BLCKTHRN.OVL` | 3,184 | Blackthorn(暴君)相關事件 | 🔶 審問已實作(FM Towns 的 `sub_C414`,`docs/re/28`);DOS overlay 本身未逆 |
| `SHOPPES2/3.OVL` | 2,848 / 2,528 | 商店(續) | ⬜ |
| `ENDGAME.OVL` | 2,800 | 結局 | ⬜ |
| `OUTSUBS.OVL` | 2,464 | 地表副程式 | ⬜ |
| `FLAMES.OVL` | 32 | 火焰動畫(極小,可能只是資料) | ⬜ |

**程式碼量級**:`ULTIMA.EXE` 36 KB + 24 個 OVL 約 130 KB 的 16-bit 機器碼。
FM Towns 版反編譯出 **61,364 行 C / 1,225 函式** —— 那就是要還原的行為總量。

## 2. 機制逐項盤點

### 2.1 世界與移動

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| 地表世界地圖 256×256 | `BRIT.DAT` 205 chunk + `DATA.OVL` 0x3886 索引表 | ✅ 完整組出 |
| 地底世界 | `UNDER.DAT` 65,536 B(256 chunk,不省略) | ✅ 已解碼並接進引擎(`internal/u5data/map.go`,`State.Under`);可走、可視線遮蔽 |
| NPC 與排程 | 四檔各 4,608 B = 8 地點 × 576 B(32 × 16 B 排程 + 32 生物編號 + 32 對話號碼) | ✅ **全解**(docs/re/04 + **29**):排程 slot 選擇、生物編號→tile(+256)、對話號碼分派、**第四個欄位「行為型別」八種**(遊走 / 怕生 / 搭話 / 跟隨 / 醉步 / 敵對)。全遊戲 325 個 NPC、46 個商人 |
| 裝備與生物名 | `DATA.OVL` 0x1806 / 0x1866 兩張 48 筆 u16 指標表(指標要 +0x10) | ✅ **已解**(docs/re/09):48 件裝備名(分兩段交錯)、角色紀錄 0x19–0x1E 的裝備欄位、生物名索引 =(編號−64)/4 |
| 商店 | 8 種店、47 家;`byte_4185C`/`off_4145C`/`off_4165C` 三張稀疏表 + `SHOPPE.DAT` 問候語 | ✅ **八種店全部可跑**(docs/re/08、10):店名店主與 24 張表都從 DATA.OVL 讀;七個佔位符全解;買 / 賣 / 療傷 / 住宿 / 寄放同伴 / 點餐 / 買馬買船。✅ **酒館的打聽消息已做**(`docs/re/61`):26 個主題(八德 + 七座地牢 + 三信物 + 三碎片 + 四雜項)、人名 / 地名 / 價格四張表在 `DATA.OVL` 0x4C84 起**連續無縫**,四句線索模板隨機挑一句。比對是**純子字串**(打 honesty 也中)—— 原版那段「前一字元是不是空白」的檢查兩個分支都落到同一個 `mov`,是死碼。⬜ 只剩坐騎物件 |
| 存檔 | `SAVED.GAM`/`INIT.GAM` 各 4,192 B:16 筆 32 B 角色紀錄 + 時間 + 隊伍 + 位置 + 業報 | ✅ **已解**(docs/re/07):逐欄位循序讀寫,位移用累加算出並以「最大HP=等級×30」交叉驗證。遊戲已能由原版存檔開局。⚠⚠ **月石那一段更正了**(`docs/re/71`):原本寫「`0x029A` 起 **16 B**,十六顆月石,0xFF = 在手上」—— **長度夾對了(下一欄是 0x02AA 藥草),顆數與語意錯了**。`sub_1A2F8` 埋月石那四行連寫 `byte_3E040/48/50/58[i]`,四個陣列各間隔 8 B ⇒ **八顆 × 四欄**:X(**0x028A**,原本整段沒解碼)/ Y(**0x0292**,同)/ **埋在哪個地點**(0x029A)/ **埋在哪一層**(0x02A2)。第二個獨立佐證:`sub_1E8D4` 建 U 的清單只掃 `ecx < 8`,而且拿 `byte_3E050[i] == 0FFh` 當「這顆拿得出來」。⇒ `0xFF` **不是**「在手上」的旗標,是「地點欄還沒被寫過」;原本那份布林讀法會把**埋在地點 0(大地圖)的月石讀成不存在** —— 而因為「沒撿到」也讀成不存在,症狀正好被掩蓋。已加一條測試專驗「地點 0 ≠ 在手上」 |
| 對話腳本 | `.TLK` 記錄是腳本:0x81–0x9F 是指令,由 `sub_1C3F8` 的 31 路跳表解譯 | ✅ **全解**(docs/re/06):指令集、`2i+5`/`2i+6` 關鍵字對與 **0x90 終止**、子字串詞首比對、34 個內建關鍵字(含髒話)、提問區塊與是非分支、入隊。135 段對話 1,307 個關鍵字全數可答 |
| 文字壓縮 | 118 字詞典(DATA.OVL 0x104C)+ 128 槽表(10 個空槽) | ✅ **全解**(docs/re/05):`.TLK` 與 `.DAT` 極性相反。SHOPPE.DAT 194 筆零殘留 token |
| 場景地圖 | 四檔各 16,384 B = 16 張 32×32(列寬由 `sub_86C` 證實) | ✅ **64 張全解出;每張屬於哪個地點/樓層已用梯子拓撲獨立驗證**(不重疊不留空)。畫面驗收:進不列顛城南門、爬上燈塔頂層燈室 |
| 地點類型 | `DATA.OVL` 0x2AB3 列 9 種;`sub_2D72C` 分派 12 種;**lighthouse 由四座燈塔的座標反推出是 tile 27** | ✅ tile 對應已知 |
| 進出場景(portal) | ✅ **已實作**(`internal/game`):E 進入、走到邊界問是否離開、離開回城門格、ARARAT 通地下世界。原版分派 `sub_2D72C` 依腳下 tile:16=hut、17=法典聖壇、18=keep、19=village、20=towne、21=castle、22=cave、23=mine、24=dungeon、25=八德聖壇、**27=lighthouse**、61=Blackthorn 宮殿、62=LB 城堡 | ✅ 規則全解 + 已接進引擎 |
| 地形通行規則 | **已從原版執行檔取得**:`byte_5FF6C` 64 B bitmap(阻擋 195/512)+ `sub_2A610`/`sub_2A674` 判定式,見 `docs/re/02` | ✅ 已接進引擎(走進海裡會顯示「受阻!」) |
| 移動成本分級 | `sub_2D0BC` + `sub_2E24` + `sub_29304` | ✅ **已實作**(`docs/re/38`):草地 0 級、沼澤/灌木/荒漠 1 級、林/丘/山 2 級;代價是**多跑 1–2 個完整世界回合**(怪物照走)+ 多 2 / 4 分鐘;那一步遇上事情就不印慢 |
| **移動者→模式表** | `byte_5FF8C[mover>>2]` + `jpt_2A6B4` 全 11 case | ✅ **全解**(`docs/re/47` + **`docs/re/51` §3 更正**):64 項、六個載具錨點同時對上。⚠⚠ **逐 case 讀跳表後發現四個模式我寫錯了**:飛行不是「什麼都不管」而是**過不了水**;大船是 `tile <= 2`(連 0x6x 那族水都進不去,這才是「靠不了岸」的機制);坐騎多擋的是**沼澤**不是水;模式 7–10 是**只認一種地形**(最嚴格),我當成兩棲(最寬鬆)。另補:`sub_2A610` 不只是位圖(還有 0x90 那組地形的規則)、小艇要查**河道流向**、default 是**擋住** |
| 世界環繞(wrap) | 已實作並測試 | ✅ |
| 方向 | `DATA.OVL` 0x2C8C:`North East South West` + `Dir:` | ✅ 中文化(北/東/南/西) |

### 2.2 時間、天候、天象

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| 時段 | `DATA.OVL` 0x7836 | ✅ 早就實作(`u5data.TimeOfDay`,邊界 12 / 18 時,接在商店對白的 `@` token)。本輪補上**中文化**:`i18n.TimeOfDay` —— 譯文正確保留了 `@`,但代入的是英文,玩家會看到「morning好!」 |
| 日夜與時鐘 | NPC 排程依賴它(`*.PTH`) | ✅ **已實作**(`docs/re/09`):分鐘級時鐘、NPC 排程、月相 |
| **日照半徑** | `sub_29304` 的 `loc_29463`;漸變表 `byte_5FF5C` | ✅ **已實作**(`docs/re/31` §5):白天 50 / 夜 2 / 日出日落六段漸變 / In Lor 18 / 火把 10(取亮不相加);地下與亞拉臘號恆暗 |
| **視線遮蔽** | `sub_2E0E8` → `sub_2DDB0`;`sub_2E1D0`(19 擋 + 5 貼著才透);距離表 `byte_601F0` | ✅ **已實作**(`docs/re/31`):flood fill 照抄、牆擋得住、Wis An Ylem 穿牆、戰鬥與石室不遮蔽 |
| **場景照明** | `sub_2E21C`;發光地形 `dword_601E4`(10 筆);光源半徑² 10 | ✅ **已實作**(`docs/re/31` §6):火盆 / 營火 / 月門各自照亮一圈,而且照不進隔壁房間;夜裡視野縮成九格,靠光源才看得遠 |
| **燈塔的光束** | `sub_10738`(找 tile 0x1B)+ `sub_2E944` → `sub_2E8E8`;扇區表 0x4120C | ✅ **已實作**(`docs/re/31`):十六個扇區排成羅盤、三個扇區寬、每幀轉一格,白天不畫。`BRIT.DAT` 裡 0x1B 剛好四個 = 四座燈塔 |
| 月相與月門 | `DATA.OVL` 月相表 + 存檔的八個目的地 | ✅ 已實作(`game/moongate.go`,`docs/re/22`):月相推進、踏上月門傳送 |
| **風向**(航海) | `sub_2A984` 設定 / `sub_2D38` 延遲表 / `sub_2D2D0` 漂流 | ✅ 早就實作(`game/wind.go`):五種風、風值代表**推向哪邊**(不是從哪來)、延遲表、**逆風完全動不了**、側風要兩三拍才走一格。這就是 U5 航海的節奏,也是 Rel Hur 有用的原因 |

### 2.3 角色與隊伍

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| **休息(H 紮營 / 睡床 / 修船)** | `sub_2B8CC` / `sub_16BA0` | ✅ **已實作**(`docs/re/50`):同一鍵三件事,**先看載具再看地點**。船上修船(揚帆修不了、五個世界回合、do-while 一定修到 10 以上、上限 99);城裡要站在床(tile 0xAB)上;地表與地牢紮營(水上與騎乘擋掉、問 1-9 小時、兩人以上才問守夜)。✅ **紮營遭遇已補**(`docs/re/51` §1):每過一個遊戲小時擲 1/64、八種野外怪物(巨鼠佔兩格)、**守夜的人在開打前把全隊叫起來**(沒守夜就躺著被打)。⚠⚠ **恢復規則三處更正**:睡不到 6 小時完全沒用、回的是 `random(1,63)` **不是回滿**、**守夜的人什麼都不恢復**;中毒者不恢復但**不會死**(毒死是旅店的規則)。休息冷卻 14 小時、每小時減 1。**睡床什麼都不恢復**,只是讓時間過去,起床往東挪一格。⚠ **照抄了原版的 off-by-one**:睡床跨午夜減 23 不是 24 會多醒一小時,而**紮營那條減的是 24** —— 同一個遊戲兩條路一條對一條錯,所以那是真的寫錯 |
| **創角(吉普賽問答)** | `QUESTION.DAT` 30 筆 + `CREATE.16` | ✅ **已實作**(`docs/re/39`):八德淘汰賽 4→2→1 共七題、勝方三圍**累加**(⚠ Hex-Rays 反編成賦值,真值是 `add`)、魔力=智力、力量下限 20、28 題對照表從執行檔抽出。⬜ 主選單與 `CREATE.16` 插圖未做,先用 `-create` 旗標進入 |
| 屬性 STR/DEX/INT | `DATA.OVL` 0x33F1 | ✅ 存檔已讀寫、升級會加(`game/levelup.go`)、聖壇冥想會用;**創角初值也已實作**(`game/create.go`:勝方三圍累加、魔力 = 智力、力量下限 20)。⚠ **更正**:此前記「創角初值分配未做」是錯的 —— 它就是上一列描述的那件事 |
| **等級與經驗** | `DATA.OVL` 0x3529;`sub_10C34`(老人) | ✅ 早就實作(`game/levelup.go`):門檻 100 / 200 / 400 / 800 / 1600 / 3200 / 6400、經驗上限 9999、等級上限 8,而且**升級是事件**(只在休息後由老人重算),不是打完怪就跳。✅ **那兩個字面找到了,而且不在 Ztats**(`docs/re/61`):它們在**轉入 U4** 的報告畫面 `sub_7594` 裡。順帶發現 `docs/re/55` 只逆了第一階段 —— 第二階段會把經驗 /10、等級由它反覆折半算出、HP = 等級 × 30、三圍走三段曲線,**而且只有力量有下限 20**。已補上(`U4TransferStat` / `Convert`)。✅ **兩個確認問題做了**(`docs/re/61` 追記):`Keep this name?`(N → 問新名字,上限 8 字元,**什麼都不打保留原名**)與 `Keep same sex?` —— ★ 後者的 **N 是翻轉不是「設成女」**,寫成「Y=男/N=女」的話女角色答 Y 會變成男的。新增通用文字提問 `AskText` + `PromptText`。⬜ 只剩那一頁一頁的**版面**(固定欄位座標、`Class remains intact` 之類的說明句) |
| **從 Ultima IV 轉入角色** | `sub_71D0` + `jpt_7349` | ✅ **已實作**(`docs/re/55`):讀 U4 `PARTY.SAV` 的**兩塊**(0x008 起 39 B 的角色記錄、0x140 起 182 B 的聖者判定)。★ 兩塊的欄位位移**相同**(都是 +6 起),混在一起就會得到「三圍全為 0 才是聖者」—— 而那對任何合法角色都不成立,結論本身就在說讀錯了。**追指標,不要追位移。**★ 八個職業 M/B/F/D/T/P/R/S 正是 U4 的八種,而且 **D/T/P/R/S 原樣寫進 U5**(原版沒摺回四種)。★ **等級 = 最大生命 / 100**,不是照經驗值算(用來釘住有沒有照抄)。驗證界線 70 / 9999 / 7 且**剛好等於要放行**(`ja` 不是 `jae`);失敗照原版只印一句。路徑用 `-u4save` 給(原版寫死 `a:party.sav`),不給就照實說 |
| **性別** | `DATA.OVL` 0x33F1;存檔 0x0B / 0x0C | ✅ 早就實作(`u5data.GenderMale/Female`,建角時寫入、Ztats 讀得到)|
| **Avatar 身分判定** | `sub_7594` 的 `dword_54498`;`sub_71D0` 設定 | ✅ **已實作**(`docs/re/54`):Ztats 末行印「乃聖者 / 並非聖者」。★ 那個旗標**唯一的來源**是從 Ultima IV 轉入角色 —— U4 存檔裡那八個 word 全為 0 才設起來。**新建的角色一律「並非聖者」,那是原版行為**,不是缺預設值(U5 開場正是「你回來了,但不是以聖者的身分」)|
| **角色數值畫面(Ztats)** | `ZSTATS.OVL`;`sub_7594` | ✅ 早就實作(`docs/re/41`):Z 開啟、左右翻頁、ESC 收起;名字職業 / 狀態 / 三圍 / 生命 / 等級經驗 / 魔力 / **聖者身分**七行 |
| 狀態 | `DATA.OVL` 0x0919 | ✅ 五種狀態都在用(`u5data.StatusGood/Poisoned/Dead/Asleep/Charmed`):中毒、睡著、死亡、休息、復活都吃它 |
| 隊伍成員招募 | NPC `join` 關鍵字 | ✅ 已實作(`game/talk.go`:opcode 0x84 → 入隊、隊伍滿了的回絕) |

### 2.4 戰鬥

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| 戰鬥地圖 | `BRIT.CBT` 5,632 B / `DUNGEON.CBT` 39,424 B | ✅ 已解碼(`u5data/combatmap.go`);戰鬥與地牢房間都在用 |
| 戰鬥流程 | `COMBAT.OVL` 7,408 + `COMSUBS.OVL` 5,216 | ✅ 完整回合已實作(`docs/re/15`/`16`):遭遇生成、敏捷排程、敵人 AI、命中與傷害、經驗值;DOS overlay 本身未逆(結論來自 FM Towns) |
| **攻擊方位** | `DATA.OVL` 0x2DA2;`sub_5008`(唯一呼叫者是 `sub_5150`) | ✅ **已實作**(`docs/re/48`)。⚠ **這是地牢的事,不是地表戰鬥** —— 唯一組出這句的是地牢踩踏分派器。方位用怪物的上一格算,`& 7` 因為一層 8×8 會繞回;**面對著它時不報方位**(看得見就不用講),報了則**隊伍轉身面向它** |
| ~~連擊次數~~ | `DATA.OVL` 0x9AA8 | ❌ **誤標,本表刪除**。`two`..`six` 的擁有者是 `sub_20ED0`,印的是旅店報價「for the **four** of ye」——**按人頭算錢的量詞**,與連擊無關。`Attack-` 只是字串池裡的鄰居。⚠ 相鄰字串不代表相關功能(`docs/re/48` §1) |
| **地牢遊蕩怪物** | `sub_4460`/`4594`/`4C6C`/`5008`;`byte_3F3D0[8]` | ✅ **已實作**(`docs/re/48`):每層一隻、走一步撲一步、**生成排除隊伍的整行整列**(所以換層後第一步安全)、不肯原地踱步(回頭要 1/8)、收割者紮根不動;睡著的隊員每步 4/64 醒 |
| **地牢戰場** | `sub_FE48`→`FD54`→`FC7C`→`FAC4`/`FB6C`/`FBF0` | ✅ **已實作**(`docs/re/48` §6)。★ **不在 `DUNGEON.CBT` 裡** —— 那 112 張全是房間,遭遇戰的戰場是**當場畫的**。四面牆照你站的路口開口(通道 7 格 / 房間門 5 格 / 牆整條封死),中央擺腳下的地形(梯子 / 寶箱 / 噴泉),牆地板依地牢分兩組。順帶解掉 `combatmap.go` 留很久的「列 1/2/4/5 那批未解資料」= **四個入場方向**,本專案原本只用其中一組 |
| **怪物 22 種以上** | `DATA.OVL` 0x0380/0x0404/0x0469 | ✅ 名字與三圍都已接(`u5data/combatstats.go`,`docs/re/15`);遭遇生成、AI、命中傷害在跑 |
| **怪物圖 `MON0–7.16`** | `off_41BC0[記錄[0]]`,索引 = `random(0,7)` | ✅ **解掉「為什麼剛好八組」**(`docs/re/48` §3):它不是八組怪物美術,是**地牢遊蕩怪物的名冊** —— 巨鼠 / 蝙蝠 / 巨蛛 / 幽魂 / 泥怪 / 小鬼 / 凝視魔 / 收割者,八個名字全部從生物名表核對過。圖檔本身的解壓早就通了(`TILES.16` 的 LZW),⬜ 缺的是第一人稱透視怎麼擺 |

### 2.5 魔法

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| 咒語(符文組合) | `DATA.OVL` 0x0919 / 0x06EE | ✅ 咒語表全解(`u5data/spells.go`,`docs/re/17`),**48 / 48 個效果已實作** |
| 法術代碼表 | `DATA.OVL` **0x09A5**:**48** 筆代碼(`IL GP AZ AN M AY AS ACX HR…`)| ✅ **已解**(`docs/re/58`):每一筆是**咒語名各詞首字母排序後**的樣子(`Rel Hur` → `HR`、`Vas Lor` → `LV`、`In Vas Por Ylem` → `IPVY`),48 筆逐一驗過 0 例外。⚠ **更正**:此前記「0x09B3、98 項」是錯的 —— 0x09B3 落在第 6 筆上,98 是把後面的地名一起數進去了 |
| **材料(reagents)** | `DATA.OVL` 0x06EE | ✅ 早就實作(`u5data.ReagentNames` 八種、`Placeholders` 的價目、調藥的配方遮罩)|
| 施法流程 | `sub_1994C` / `sub_1CA0C`;`CAST.OVL` 8,560 + `CAST2.OVL` 4,544 | ✅ **輸入法已改對**(`docs/re/58`):原版**打符文首字母**(最多四個,回顯整個符文詞,Enter 或空白送出,J / O 默默丟掉),不是打咒語全名 —— 舊版的 `CastByName` 是我自創的,已移除。✅ 地點分派全解,含**兩個「被吸收了!」的地點**(第二座城堡未拿王冠 / `STONEGATE`),而那正是王冠與權杖的所在。⬜ `CAST.OVL` / `CAST2.OVL` 未逐位元組對照(流程是從 FM Towns 逆的)|
| **Words of Power**(地牢咒語) | `DATA.OVL` 0x44AD | ✅ 已實作(`u5data/wordofpower.go`,`docs/re/26`):八個字 + Yell 說出的判定 |

### 2.6 物品、裝備、經濟

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| 裝備 48 項 | `DATA.OVL` 0x0052 | ✅ 名字、戰鬥數值(防禦 / 射程 / 類別)、六個裝備欄位、換裝七條規則(`docs/re/09`、`42`、`72`)。⬜ `byte_3E0B2` 的 0x10 位元語意未解(卸戒指時要清它,時機知道語意不知道 ⇒ 引擎先不補假欄位);`sub_2F35C` 當黑盒 |
| **特殊道具名表** | `DATA.OVL` **0x1946**(指標表;0x04C3 只是字串位置) | ✅ **已實作**(`docs/re/56`)。⚠ **「158 項」標錯了** —— 那是整張表的長度,不是 158 種特殊道具。★ 真相是**同一批裝備有兩套名字**:0x1806 是長名(`Leather Helm`)、0x1946 第 22 筆起是**短名**(`Leath Helm`,48 件裡 28 件不同)——後者是背包面板用的,因為 U5 的面板很窄。前 22 筆才是 U 指令的特殊道具,索引 = case 編號 − 16。表是用**反向查指標**找到的(全檔只有一處指向 `Magic Crpt`),不是從既有表往後推位移 |
| **U 的清單(38 格)** | `sub_1E8D4`(建表)/ `sub_1A5E8`(分派)/ `sub_19ED8` / `sub_1A0B0` / `sub_1A2F8` | 🔶 **38 格已全部接上**(`docs/re/71`、`77`):卷軸 8 / 藥水 8 / 月石 8 / 碎片 3 + 信物與雜項。⬜ **埋下去的月石怎麼變成月門**未追(`sub_E084` 讀的是另一組表)⇒ 月石唯一的用途還沒閉環;「On who:」隊員選單仍是近似 |
| **魔法飾品** | 就是裝備 42..47 | ✅ 早就實作 —— `ItemNames` 讀得到 `Ring of Invisibility / Protection / Regeneration`、`Amulet/Turning`、`Spiked Collar`、`Ankh`,而 `SlotFor` 把它們分到指環與頸飾兩個欄位(`docs/re/42`)|
| **商店店名與店主** | `DATA.OVL` 0xBFE(46 個店名)/ 0xEFA(47 個店主) | ✅ 早就實作(`docs/re/08`/`10`):三張稀疏表 + 字串一律讀玩家自己的檔。⚠ 47 家店只有 46 個店名 —— 地點 30 的馬廄沒有,那是原版資料就這樣 |
| 商店對白 | `SHOPPE.DAT` 194 筆 | ✅ 已解碼並展開詞典;推銷詞 / 收購說詞 / 問候語的位移表都在 `DATA.OVL` |
| 商店流程 | `SHOPPES.OVL` + `SHOPPES2/3.OVL` 共 11,312 B | ✅ 已由 FM Towns `WORRIORS.EXP` 逆出,DOS 資料表逐位元組對上(docs/re/10) |

### 2.7 NPC 與對話

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| NPC 對話文字 | `*.TLK` ×4 | ✅ 解碼(48 筆/檔,bit7 編碼);🔶 欄位切分未定 |
| NPC 定義 | `*.NPC` 各 4,608 B | ✅ 已解碼(`u5data/npc.go`):32 個居民 × 生物碼 + 排程 + 座標;含 NPC→物件鏡射(`docs/re/36`) |
| **NPC 排程與路徑** | `.NPC` 的 16 B 記錄;`sub_8924`/`9690`/`91A4`/`8A1C`/`8BA0`/`8D28`/`9428` | ✅ **已實作**(`docs/re/12`):四時段作息、環狀佇列有界尋路、兩張互為相反的方向表。⚠ **`BRITISH.PTH` 不屬於這一列** —— 它由主選單的 `sub_6730` 載入 4000 B 緩衝(`briBuffer`),每個位元組是一步(低四位元 dx、高四位元 dy,bit3 是正負號),`sub_5648(x,y)` 一路走到指定座標。那是**開場動畫掃過不列顛尼亞的路徑**,不是 NPC 尋路。⬜ 開場動畫本身未做 |
| **NPC 職業** | `DATA.OVL` 0x0342 | ✅ 早就實作 —— 它與生物名表(0x1866)是**同一批名字**:村民 / 商人 / 弄臣 / 吟遊詩人 …(`u5data.CreatureTable`,索引 4..13),`i18n/names.go` 有中譯 |
| 對話關鍵字系統 | `TALK.OVL` 4,880 B | ✅ 31 路指令集全解(`docs/re/06`),**含原本標「未定」的 0x88「汝名為何?」** |
| 招牌 | `SIGNS.DAT` 8,364 B | ✅ **全解 + 全譯**(`docs/re/37`):78 塊,u16×33 位移表 + `[地點][樓層][x][y]` 記錄;框是 `RUNES.CH` 的字模美術,字是真文字 |
| 觀察(look) | `LOOK2.DAT` 3,622 B | ✅ **全解 + 全譯**(`docs/re/37`):u16×512 位移表,地形查 `[t]`、物件查 `[t+256]`;L 指令含時鐘報時、聖火、封印地牢、噴泉、水晶球、看天 |

### 2.8 業報與美德

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| 業報(Karma) | `KARMA.DAT` 761 B / 6 筆 | 🔶 文字已解;增減點已接(聖壇、對話),完整規則待逆 |
| 聖壇與真言 | `sub_1D394`;`MISCMSG.DAT`:`"What is the Mantra of the Mystic Shrine of` | ✅ **已實作**(`docs/re/25`):八座座標、真言前綴比對、三次真言、試煉旗標、三圍與業報獎賞、獻金 |
| **力量之言** | `sub_17CFC` / `off_55DF8`:`FALLAX VILIS INOPIA MALUM AVIDUS INFAMA IGNAVUS VERAMOCOR` | ✅ **已實作**(`docs/re/26`):西南東北掃鄰格、XOR 切換地牢封印、復原被玷污的聖壇 |
| **Yell 指令** | `sub_17E74`:船上收放帆 / 城裡喊暗影君主 / 野外力量之言 | ✅ **已實作**,按鍵 Y |
| 寶典 | `sub_1D850`;答案是 `MISCMSG.DAT` 第 20..27 筆 | ✅ **已實作**(`docs/re/27`):領最小編號的試煉答案、湊滿八德播四段符文;聖壇的簡化已拿掉 |
| 聖壇 / 寶典石室 | `sub_1DA10`;`MISCMAPS.DAT` 位移 0xB0 / 0x160 | 🔶 規則已接(依地形分派、進出走 16 分鐘);11×11 石室的畫面未做 |
| 八德 | 系列共通;U5 的政治主題圍繞它 | 🔶 聖壇那一半已完整 |

### 2.9 地牢

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| 地牢地圖 | `DUNGEON.DAT` 4,096 B | ✅ 全解(`u5data/dungeon.go`,`docs/re/18`):八座 × 8 層 × 8×8 |
| 第一人稱視角 | `DUNGEON.OVL` 8,016 + `DNGLOOK.OVL` 5,040 | ✅ **透視在跑**(`u5data/dungeonview.go` + `render`,含走廊裡的梯子 / 寶箱 / 噴泉,並有左右對稱的截圖測試)。⚠ **更正**:此前這一列掛 ⬜ 與下一列的 ✅ 自相矛盾 —— ⬜ 指的是那兩個 `.OVL` 還沒逐一反組譯(已另列在 §1),不是透視沒做。⬜ 精確的內縮量仍未讀(引擎目前置中) |
| 地牢圖素 | `DNG1–3.16` / `DNG1–3.PNL` | ✅ DOS 的 `DNG1–3.16` 已解(`u5data/dungeonview.go`),第一人稱透視在跑(含走廊裡的梯子 / 寶箱 / 噴泉) |
| 地牢戰鬥 | `DUNGEON.CBT` 39,424 B | ✅ 已解碼並接上(房間戰鬥走同一套戰鬥流程) |

### 2.10 載具

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| **載具動詞與朝向** | `sub_7C0` | ✅ **已實作**(`docs/re/54`):騎行 / 飛行 / 划行(船不印動詞)。★ 同一支還改**朝向位元**:馬與魔毯**只在東西向換圖**,船與小艇是四向全換 —— 兩種規則不同。而朝向位元同時是開砲判舷側(`isBroadside`)與通行判定(`ModeOf`)讀的東西,不更新就會算錯 |
| 馬 | `sub_16F08` / `sub_177AC` | ✅ 上下馬、通行判定、買馬都已接(`game/vehicle.go`)。✅ **彩蛋找到了,而且是一整個沒實作的機制**(`docs/re/65`):那組名字屬於**許願井**(`sub_CD28`,Look 撞到 tile **0xA1**)—— 投一枚錢 → 問「Thy wish?」(收 12 字)→ 比對 `CORVETTE / FERRARI / LAMBORGHINI / LOTUS / PORSCHE / HORSE` → 若地點是 **22 PAWS** 或 **31 EMPATH ABBEY** 就印 `Poof!` 並在**正東一格**生一匹馬(`sub_2B6C8(10h,10h,X+1,Y,樓層,0,空槽)`,與買馬**逐位元組相同**)。★ 比對是「**大寫字面值的前綴**」(`sub_27C98` 轉大寫後 `strncmp` 字面值長度)→ `HORSEY` 算對、`HORS` 不算、**小寫不生效**(照原樣保留,不順手修)。★ 沒錢時原版**一句話都不印**,看起來像卡住 —— 也照原樣。⚠⚠ **錯因記帳**:此前 `strings.go` 寫「原版不管答什麼都沒有後續,組語裡沒有任何寫入」,因為讀的是 **Hex-Rays 截斷後的 `sub_CD28`** —— 它產出一段看起來完整、會 return、毫無警告的函式,而組語版有三個參數、四倍長。⇒ 判斷法補一條:**反編譯出來的函式參數列是空的、而呼叫端明顯在推參數 → 就是被截斷了**(`CLAUDE.md` §4.4)。落地 `u5data/well.go` + `game/well.go`,八條測試(三條專擋回歸) |
| **大地圖攀爬(K)** | `sub_188C4` | ✅ **已實作**(`docs/re/68` 追記):引擎原本寫「大地圖上的攀爬是另一條路徑,**還沒做**」。前提是**抓鉤** + **只有步行**(又是單一值 0x1C);目標地形 **0x0C 群山**才能爬,**0x0D 峭壁**印的是另一句 `Impassable!`;每個沒死的隊員擲 `rand(1,30) > 敏捷` 就 `Fell!` 並受 rand(1,5) 傷,★ **摔倒不擋移動**(原版的移動在迴圈之後、無條件執行)。★★ 順帶更正 `byte_3DFBB`:它**不是繩索,是抓鉤(Grapple)**,而且**位移早就對出來了**(0x0209,就是 save.go 那段「兩端都已釘死」的七個位元組的第一格)——`hasRope()` 的 `return false` 是**陳舊標記**,而它讓大地圖攀爬與「用抓鉤從洞爬回上一層」永遠不可能發生。已補 `Inventory.Grapple` + 存讀檔。✅ **`sub_10A1C` 的墜落也做了**:觸發是**瀑布**(`tile & 0xFC == 0xD4`,`look#212..215` 四格都是「瀑布」),兩處觸發(移動後腳下、漂流後往南一列)。傷害是**固定 1 點**不是骰的(`sub_2B724` 只決定有沒有受傷 → 敏捷 31 以上永遠不受傷);**通往幽冥界的座標寫死在程式裡 (54, 138)**,世界上只有那一個瀑布下得去,其餘瀑布只是掉一次扣一點血。★ 那個畫面效果**有還原**載具碼,與 `sub_22F0` 溺水那段的「設 0 不還原」成對 —— 互相印證那個 0 是「不畫載具圖」 |
| **開砲(F)** | `sub_172C4` / `sub_2B3DC` | ✅ **後半補上了**(`docs/re/69`):⚠⚠ **大地圖上開砲原本只印一句 BOOOM,砲彈根本不飛** —— `fireToward` 呼叫的 `FlyProjectile` 第一行就是 `if s.Combat == nil { return }`,而註解寫著「與投射物同一條路」(在戰場上成立、大地圖上完全不成立)。沒有任何測試會發現:訊息印了、函式回傳了、沒有錯誤。原版那 244 行有 200 行被截斷成三行。現在:砲彈最多飛 **5 格**,每格**先問物件再問地形** —— 打到**七種門**(0x97/0x98/0x99/0xB8..0xBB,`look#` 七個都是門)→ `Door destroyed!` 且那格變 **0x44 磚地**;打到有效目標 → **整個物件槽直接清掉**(沒有血量、不進戰鬥,一砲一個)+ **業報 −5**(下限 0)+ 若是 NPC 就**被逮捕**;打到**槽 0(隊伍自己)→ 全隊受傷而且不扣業報**。★ 有效目標的判準:`kind == 0x10/0x11`(**馬**,編號比 0x1C 小卻明文放行)是目標;`kind < 0x1C` 不是;**`kind & 0xF8 == 0x78` 不是 —— 那是黑刺(14)與不列顛王(15)**,砲打不掉。⚠ 中間那道 `and eax,0FCh; cmp eax,2Fh` 是**死碼**(`& 0xFC` 清掉低兩位,永遠不可能等於 0x2F)→ 船照樣打得掉,已用測試釘住免得有人「照著註解」把船排除。⬜ `byte_4198A` 的位元語意、`sub_20CB4` 軌跡演出、`sub_2E0`(槽號→NPC)與引擎 `NPCAt` 是否等價未驗 |
| **地形效果(每回合)** | `sub_1318` | ✅ **已實作**(`docs/re/70`、**`81`**)。⚠⚠ **`sub_1318` 只在場景裡跑** —— 大地圖走 `sub_2D9D0` 自己那一串(`docs/re/81` §1)。引擎目前所有模式都跑 `terrainEffects()`,要分流;沼澤的兩顆骰子區分的是**地點**不是「走進去 vs 站著」(`docs/re/74` 已就地更正) |
| **喝醉會走不直** | `sub_21108` / `sub_1158` | ✅ **已實作**(`docs/re/70` 追記二):⚠ `tavern.go` 原本寫「原版只把『這趟喝了幾杯』加一,**沒有其他效果**」—— 錯的。同一支 `sub_21108`(酒單,也在截斷清單上:153 行 → 6 行 C)還有 `mov byte_3E169, 19h`,而 `byte_3E169` 是**讀鍵那一層**(`sub_1158`)在看的:**一半機率把按到的鍵換成一個隨機方向**、印 `Hic!`、**踉蹺了才扣一次**(所以平均要按五十次才醒)。方向來自 `byte_4FC54` 的前四個位元組 `3,4,2,1` = 四個方向鍵碼。★ 這是**輸入層**的效果:玩家按 Z 想看數值,結果往東走一格。少了它酒館的酒只是「花錢買一句話」。攔在字母指令與方向鍵**之前**,才會像原版一樣所有鍵都會踉蹺 |
| **維生開銷(餓 / 中毒)** | `sub_2A50C` | ✅ **已實作**(`docs/re/70`)。四個呼叫點分屬四個互斥迴圈(場景 / 大地圖 / 地牢 / 睡床),**每回合只跑一次**,⬜「中毒掉 2 血」已結案(`docs/re/81` §2)。★ 附帶:**戰鬥中不算維生開銷** |
| **戒指會消失** | `sub_2EE84` | ✅ **已實作**(`docs/re/70`):開戰佈陣的迴圈裡夾了一段與佈陣無關的判定 ——戴著**隱形戒指(0x2A)**或**再生戒指(0x2C)**的人,每次開戰有 **1/16** 機率戒指消失(`A ring has vanished!`)。★ **防護戒指(0x2B)不會** —— 原版只比那兩個值,中間那個跳過去,而會消失的正好是效果最強的兩個。名字從 `DATA.OVL` 0x1806 的指標表逐筆解出來。⚠ 判準是 `rand(0,15) == 11` 不是 `< 1` —— 機率一樣但**隨機序列不同**,照原樣。⚠ 寫測試時踩到:每次重建 State 再擲一次,在固定種子(`SeedRandom` 預設 1)下**等於只擲了一次**,測試會假紅;要重用同一個 State |
| **橋下的食人妖** | `sub_3010` / `sub_2F48` | ✅ **已實作**(`docs/re/68`):踏上橋(tile 0x6A/0x6B)有 **1/8** 遇到,而且**只有步行**會遇到(原版 `cmp byte_3E08C, 1Ch` 是**單一值**,不是 `IsOnFoot` 的兩值判斷)。每個活著又醒著的隊員擲 **敏捷 >= rand(1,30)** 偷渡,**一人被抓整隊停下**;全過 → `Trolls evaded!`。被抓 → `Caught!` + 過路費 **99 − 力量×3**,收費看的是**第一個能行動的人**;不付、或付不出來(扣完變負數就退回)→ 生成種類 **0xE4**(生物索引 41 = Troll)開打。★ **偷渡擲敏捷、過路費看力量** —— `byte_3DDC1`(0x0D)與 `byte_3DDC0`(0x0C)是**相鄰**欄位,很容易看成同一個,已有測試分開驗。⚠ **原版的算式沒有下限**:力量 34 以上通行費是負數 → `金錢 -= 費用` 變成**加錢**。照原樣保留,並用 `TestTollFormulaHasNoFloor` 擋住「順手加下限」 |
| **Hex-Rays 截斷稽核** | `tools/hexrays_truncation.py` | 🔶 **110 個函式的清單,已讀 91、剩 19**(`docs/re/66`)。逐輪的發現寫在 `docs/re/67`、`71`–`81` —— **那些筆記是唯一真相,本列只記進度**。⚠ 宣稱任何階段完成前要重跑 `tools/find_unwired.py`(抓「寫完了卻沒人叫」,已抓到五次)與 `tools/check_doc_claims.py`(抓過期斷言) |
| 船 + 風向 | 0x5564 風向表;`sub_22F0` / `sub_2CCFC` / `sub_2D9D0` | ✅ 已實作(`game/wind.go`,`docs/re/23`):風向、頂風延遲、收放帆、船體耐久、小艇。★★ **整個沉船系統補上了**(`docs/re/66`)——在此之前引擎裡的船永遠不會沉,漏掉的原因是 Hex-Rays 把 `sub_22F0` 安靜截斷。`DamageShip`:只有大船受損(`& 0xF8 == 0x20`)、傷害 rand(1,30)、撐不住就 `Ship sunk!` 並走**小艇 → 魔毯 → 溺水**三層階梯(換小艇**不扣小艇數**、換魔毯扣一張且朝向隨機、溺水把載具碼設成 **0**「不畫載具圖」並讓每個沒死的隊員受 rand(1,8) 傷)。★ `Hull weak!` 門檻 **50**,與上船時的 `Danger! Ship badly damaged!`(10)是**兩個不同的警告**。★ **船轉向要花掉一回合**(`Head <方向>`),引擎原本轉向 + 前進同一步。⚠ **修掉一個錯**:`CanSail` 原本寫「無風照走」—— 不查延遲表是對的,但**揚著帆無風就是動不了**;收帆的船(0x24..0x27)才不受風影響,這也解釋了為什麼原版分成兩組載具碼。★ `Rough seas!`(小艇 / 魔毯站在水 tile 1)之後那次 `sub_22F0` 對小船**是空轉**,照原樣保留並用測試釘住。✅ **`sub_2CE70` 讀完了**(`docs/re/68`):它是**通行判定 + 撞擊**的同一支。揚著帆撞上東西 → `COLLISION!`(淺灘另外先印 `BREAKING UP!`)→ **船身受損 + 自動收帆**;撞上**碼頭** → `Docked!` 且**一句撞擊都不印**,靠岸是 `add 載具, 4`(揚帆組 → 收帆組,朝向自然保留);沒揚帆 → `Blocked!`,而**仙人掌**會 `OUCH!` 並讓全隊受傷。★ 收帆的船每走一步印 `Rowing!`(`sub_7C0` 對船不印動詞,這一句在 `sub_2CE70` 開頭)。✅ **`RoughSeasTile = 1` 是深水**:四個地形值(1 深水 / 3 淺灘 / 0x2F 仙人掌 / 0x47 碼頭)用 `look#<tile>` 一次全對上語意,而那個鍵**直接是 tile 編號**。⬜ `sub_2CE70` 中段那串「載具 × 目標種類」的通行判斷沒逐條追(引擎仍用自己的 `TileBlocksWalking` + `ModeOf`,是否等價未驗);`sub_2B360` 當黑盒;`byte_3E168` 誰讀未追 |
| 飛毯 | `Fly` + `Magic Crpt` | ✅ 上下魔毯與通行判定已接(`game/vehicle.go`);魔毯本身由 Get 撿(`docs/re/33`) |
| 小艇(skiff) | `VehicleSkiff` 0x28..0x2B | ✅ 上下小艇、船上載艇數都已接 |

### 2.11 劇情與結局

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| 開場四章 | `DATA.OVL` 0xA020:`The Summoning / The Journey / The Arrival / The Welcoming`;`STORY.DAT` 20 筆 + `STORY1–6.16` | 🔶 文字已解 |
| **三位暗影君主** | `sub_17A14`(召喚)/ `sub_29304`(遊走)/ `sub_1A38C`(消滅);`off_55DEC` | ✅ **已實作**(`docs/re/28`):召喚、午夜換城(避開玩家且不重複)、聖火前用碎片消滅(三個條件與兩種沉默) |
| **三個邪惡碎片** | `sub_1A38C`(使用)/ `sub_154BC` 0xB4(取得)/ `sub_10B3C`(擺放);存檔 0x0210 | ✅ **取得、擺放、使用全部實作**(`docs/re/28` + **33**):進地下世界時當場擺進槽 29..31(座標表在執行檔裡,不在資料檔),Get 撿起來,投進聖火消滅暗影君主。用掉的不重生 |
| **不列顛王的信物** | `sub_154BC` 0xB5/0xB6/0xB7;`sub_1E74` 鏡射;存檔 0x020D..0x020F | ✅ 王冠 / 權杖 / **護符**可撿、可用(`docs/re/44`)、存讀檔。✅ **擺放位置已解**(`docs/re/57`):兩件都是 `.NPC` 檔裡行為型別 0 的槽 —— 王冠在地點 18(第二座城堡)+3 層 (15,13)、權杖在 `STONEGATE` 地面層 (15,15),靠 NPC 鏡射被 Get 看見,引擎不需特例。⚠ **卡住的原因是命名空間**:地形 0xB4..0xB7 是**加農砲**,物件種類才是信物 —— 拿地圖 tile 去找會撈到一堆對稱的砲。⚠ 王冠配了 `sub_218` 永久移除,**權杖沒有 → 回頭再來還在原地(可以刷,照抄)**。⚠ **更正**:0x020D 此前叫「寶珠」是錯的 —— `sub_154BC` 印的是 "The Amulet of Lord British!";U5 的三件信物沒有寶珠(那是 U6) |
| **檀香木盒** | `sub_154BC` 0x0E;存檔 0x0219;`CASTLE.NPC` 地點 17 槽 31 | ✅ **已實作**(`docs/re/36`):不列顛王城堡二樓密室,豎琴彈完十三個音才開得了門;撿走後寫進永久移除遮罩 |
| Blackthorn | `sub_C414` → `sub_C318`;`BLCKTHRN.OVL` 3,184 B | ✅ **已實作**(`docs/re/28` §2 + **32**):四輪問答、招供玷污聖壇 −5 業報、拒絕到底同伴被斬、骨灰罈標記、丟進宮殿地牢;**逮捕的觸發**(對話號碼 0xFF 的攔路盤查)已接 |
| **衛兵盤查** | `sub_1B3D0`;密語 `IMPE`(FM Towns `aImpe` = DOS `DATA.OVL` 0x4AAA) | ✅ **已實作**(`docs/re/32`):三種盤查(人頭貢金 / 米諾克半數家財 / 黑棘宮殿徽章+密語)、付不出來一樣抓、徽章與戰鬥模式咒語共用位元組。✅ `sub_195C` 的第三入口(`arg_0 != 0`)已追完(`docs/re/35` §5):它**不是另一條路**,就是「盤查失敗 → 逮捕」的回程 —— `sub_1A54` 傳的是 `esi − 1`,而全檔**只有一處**把 `esi` 設成 2(按鍵 `T` 且 `sub_1B658` 談成),而 `sub_1B658` 的非 0 回傳只在「對話號碼 0xFF 且 `sub_1B3D0` 沒過」時出現。引擎 `talk.go` 在 `guardChallenge()` 回 false 時直接 `Arrest()`,行為相同、路徑短一截。⚠ 本欄此前的 ⬜ 是**陳舊標記**:`docs/re/35` 寫完當時就結案了,只是沒回來改這一格 |
| Lord British 歸來 | `sub_135FC`;`ENDMSG.DAT` 11 筆;`ENDGAME.OVL` | ✅ **已實作**(`docs/re/30`):無條件復活隊伍、問盒子(答 N 追問一次)、**兩個結局**(帶盒子才是真結局) |
| 結局畫面 | `END.DAT` / `END1–2.16` / `ENDSC.16` | 🔶 文字流程已接,**製作名單已接**(`sub_13258`:羊皮紙頒獎狀 + 歷時結算 + 兩行符文);`END1–2.16` 的插圖未做 |
| **結局的觸發** | `sub_161E4`(哨兵 `byte_3E0B0 = 'M'`)→ `sub_42CC` / `sub_163B0` 兩處查它 → `sub_135FC` | ✅ **條件解出來了,而且是死碼**(`docs/re/34`):活著的單位站在第 2 列、**正北那一格的疊圖位元組**落在 0x3C..0x3F 就被「吸收」。三個更正:`byte_3E0B0` 是**撤離方向**('M' 只是哨兵);`byte_3F854 = byte_3F844 + 16` 是**第 1 列**不是腳下;`byte_3F844` 是**疊圖層不是地形**(整塊填 0xFF,只收石室地圖與物件 tile)。0x3C..0x3F 是城堡外牆與城門,只存在於**世界地圖**,進不了疊圖層 → **原版自己就走不到這一幕**。規則照抄保留,不自己補路 |

### 2.12 系統

| 項目 | 原版證據 | 狀態 |
|---|---|---|
| 指令選單 | `sub_1F3A4` / `sub_1E418` / `sub_18468` | ✅ **已接**(`docs/re/46`):R / N / U / M 四支共用一個選單(方向鍵 + Enter);換位的第一層**故意列得出聖者**,選了才說「必須帶隊」。✅ **調藥的逐味挑選已做**(`docs/re/58`):藥草清單是**複選** —— 只列身上有的、空白 / Enter 勾選、**M 才確定**、ESC 放棄,之後問兩位數份數。⚠ 四個方向鍵在原版是「兩上兩下」,但**哪個箭頭對哪邊判不出來**,引擎取上/左為上。✅ **字母捷徑的前提是錯的**(`docs/re/60`):把原版的清單瀏覽器 `sub_1EFC8` 整支掃過,比對到的鍵碼只有 1..4 / 0xD3..0xD6 / 13 / 0x20 / 0x1B —— **0x41..0x5A 一次都沒出現**。`Item: ` 是「停在哪一項」的標籤,不是要你按字母。⇒ 引擎的方向鍵 + Enter 本來就是照原版。✅ 補上原版漏掉的**翻頁一次移 7 項**(0xD5/0xD6 → `PickPage`)。✅ **`sub_1EFC8` 整支讀完**(`docs/re/60` 追記):0xD3 / 0xD4 = **Home / End**;而同一段裡有三件比鍵碼重要的事被漏掉並已補 —— ① `arg_0` 是**視窗頂端**不是游標,視窗只有 **7 列**(畫到文字列 9 就 `break`),游標列 `j` 是另一個量(`Picker.Top` + `row()`);② ★ **游標黏在第 4 列(正中間)**,到那裡就改成捲視窗,只有頭尾捲不動時游標才自己走(開頭 1→4、結尾 4→7);③ ★ **不繞回** —— 到頂到底那一步空轉(`TestPickMoveWraps` 是量我自己加的行為,已改名 `TestPickMoveDoesNotWrap` 反向釘住,這是本專案第三次踩到同一形狀);④ ★ **捲動指示三個箭頭** `sub_29008(25/24/18)`= CP437 的 ↓ / ↑ / ↕,已落地 `PickScrollHint()` |
| 主選單 | `sub_60BC` 六項 | ✅ **已實作**(`docs/re/40`):繼續前行 / 建立新角色 / 開場 / 回到景色四項可用;**轉入 U4** 與 **製作群** 照實顯示未實作,不做假分支 |
| 存讀檔 | `SAVED.GAM` 4,192 B / `INIT.GAM`;`SAVED.OOL`;`sub_27D24`(讀)/ `sub_284CC`(寫) | ✅ **已實作**(`docs/re/07` + `docs/re/26` §6):可由原版存檔開局,寫回設定目錄的檔原版也讀得起來;未解欄位以讀進來的那份當底稿保留 |
| **交談流程** | `sub_1B658` → `sub_1B18C`(伸手夠得到的地形)、`sub_1B52C` | ✅ **已補齊**(`docs/re/35` §6):先問方向、隔著櫃檯再看一格、地形 0x9D / 0xAB 擋話 |
| **豎琴與暗門** | `sub_11E0`;音階 `dword_478C8`、曲子 `byte_4FC80` | ✅ **已實作**(`docs/re/35`):十鍵大調音階、十三音的曲子、KMP 式回退、不列顛王城堡二樓 (17,13) 的暗門 |
| **指令系統** | **`sub_2ACF4`(59 case 主分派器)** + `jpt_A5C8`(戰鬥)+ `CMDS.OVL` | ✅ **兩張指令表全解**(`docs/re/49`、`60`):主分派器 59 case、戰鬥分派器另一張;case 編號就是 ASCII 碼。⬜ 空白鍵(Pass)沒有走每回合收尾(`docs/re/81` §5) |
| **NPC ⇄ 物件表鏡射** | `sub_1E74` 配槽 / `sub_268` 放掉 / `sub_218` 永久移除 / `sub_2E0` 反查;`off_4FC98` 遮罩 | ✅ **已實作**(`docs/re/36`):城裡的寶箱、地上的物品、檀香木盒全都是 `.NPC` 檔裡生物編號 < 0x40 的槽,靠這條鏡射 Get 才看得見;順帶修掉「樓層位元組 0xFF 沒轉成 −1 → 整層地下室的東西不出現」 |
| **Get(撿東西)** | `sub_15A94` → `sub_154BC`;地牢走 `sub_15930` + 三張表 `byte_55DD4/DDC/DE4` | ✅ **已實作**(`docs/re/33`):物件與地形兩條路、碎片 / 檀香木盒 / 王冠 / 權杖 / 寶珠 / 圖紙 / 魔毯、偷食物扣業報、盤子的方向規矩、地牢寶箱獎品(開箱與取物分兩步)。**`sub_154BC` 十三條分支全部接完**:藥水(品質選顏色)、卷軸(⚠ 品質 0xFF 是攻城圖紙不是卷軸)、裝備(品質就是裝備編號,箭矢與弩矢一次 +5)、月石、⚠ 鑰匙的第二個計數欄「怪鑰匙」;順帶對出存檔位移 0x020B / 0x027A / 0x0282 / 0x029A |
| 四種顯示模式 | `EGA.DRV / CGA.DRV / HER.DRV / T1K.DRV` | 🔶 **四種全可用**(`docs/re/62`、`64`):EGA / CGA / Hercules / Tandy 熱鍵切換。⬜ 「2bpp → 單色」的轉換在哪個 `.OVL`、是否逐列換相位仍未證;`internal/render` 畫 320×200 邏輯座標,**未產生 640×300 位元圖** ⇒ 尚非逐像素重現(啟動時會印一句說明) |
| 音樂 | upgrade 19 首 `.XMI`;FM Towns 15 首 `.EUP` + 2 條 CDDA;PC-98 `UL01–15.BIN`;**播放函式 `sub_3181C(曲目)`,曲目 0–14**;已知 castle=6、LB 城堡=7、village/hut=8、Blackthorn 宮殿=11(`docs/re/03`) | 🔶 場景→BGM 對應部分已知 |
| 音效 | FM Towns 25 個 `.SND` + `U5_SE.TBL` | 🔶 **格式已解**(`docs/re/63`):32 B 檔頭(+0x0C = 檔案大小 − 32,**25/25 相符**);`U5_SE.TBL` 是純文字表 `檔名 音量 大小`,第三欄與實際大小逐一相符 —— 兩份獨立佐證。**PCM 是 sign-magnitude 有量化證據了**:平滑度指標對十個檔案全勝二補數(43.0 vs 162.2)。`u5dump snd` 可轉 23 個 WAV 用耳朵驗。✅ **載入與播放兩支都追完了**(`docs/re/63` 追記):`fscanf(f,"%s %d %d",…)` 直接證實三欄的讀法、迴圈跑**正好 23 次**、音量取 `dword_60100[索引]` 傳給驅動、通道 = `索引 & 7`(**8 個 PCM 通道**)、播放走 FM Towns 音源驅動 `AH=25h`(0x27 停止、0x28 查狀態)。★ `+0x1C` 是**基準音高**(`sub_2C4F4` 有一處「用音高 60 播第 3 號」)。✅ **更正**:此前說迴圈欄位「沒全對」是我想錯了 —— 在「起點/長度」語意下 25 個檔案**全部自洽**(長度 0、長度 1 繞單一取樣、真迴圈三種形態)。⬜ 取樣率的**單位**未定案(遊戲不管率,由檔頭與驅動決定;7926 vs `+0x18 × 4` 都合理)⬜ 尚未接進遊戲播放 |
| **打包與 CI** | — | ✅ **已做**:`tools/package.sh`(Linux tar.gz + Windows zip,全程 docker)、`.github/workflows/ci.yml`(gofmt / vet / test / 交叉編譯 / **交付包編碼檢查**)、`release.yml`(打 tag 出三平台,macOS 走原生 runner 產 universal)。交付包不含原版資料與中文字庫。⚠ GUI 實機啟動尚未在本機驗過(無顯示裝置) |

## 3. 目前完成度(誠實版)

### 3.0 兩個可重算的指標(不是感覺)

「還剩多少」用百分比講會失真 —— 子系統都接上了,但逐函式核實才是真正的長尾。
所以分兩個數,兩個都可以用一行指令重算:

| 指標 | 現值(2026-08-08) | 怎麼重算 |
|---|---|---|
| **函式覆蓋** | 反組譯 **1,232** 個函式,筆記提到 **459**(**37.3%**) | `grep -c '^sub_.* proc' …WORRIORS.EXP.asm` ÷ `grep -ohE 'sub_[0-9A-F]+' docs/**/*.md \| sort -u \| wc -l` |
| **截斷清單** | **110** 個(Hex-Rays 丟掉內容的高風險函式),已讀 **91**、**剩 19** | `tools/hexrays_truncation.py --signal both --top 5000` |

⚠ **⬜ 的數量會先增後減,不是退步。** 每讀一支函式就會冒出兩三個更細的問號 ——
盤點變細的階段,清單一定變長。真正收斂的是上面那兩個數:**截斷清單 91/110**
是目前唯一單調前進的指標,而它是「最可能藏著整個沒實作的子系統」的那一組
(卷軸 / 藥水 / 月石 / 碎片 / 風浪五個子系統都是從這份清單挖出來的)。

### 3.1 分層現況(2026-08-08 逐項對程式碼核實,不採用文件自述)

⚠ 這次核實推翻了上一版四列中的兩列 —— 中文化**不是 30%**(文字本體 99.9%)、
素材解碼**不是 70%**(只缺音樂)。⚠⚠ 而遊戲機制那一列的問題不是百分比高低,
是**驗收方式**:靜態讀碼再多也不構成「與原版一致」的證據。

| 層 | 狀態 |
|---|---|
| 素材解碼 | ✅ **實質完成**:tileset(DOS `TILES.16` 的 LZW 已破,與 FM Towns 逐位元組相同)、世界地圖、地底、64 張場景、戰鬥圖、字型、`.NPC`、存檔、50 個 `.16`/`.4` 圖檔(398 形狀)、四類文字檔。**唯一缺口:音樂**(`.EUP` / `.XMI` / CDDA 一個都沒解) |
| 顯示層 | 🔶 地圖視窗 ✅、CJK 文字 ✅、HUD ✅、戰鬥圖 ✅、地牢第一人稱透視 ✅、全景 ✅、四種顯示模式 ✅、開場動畫 ✅。⬜ 主選單插圖、石室走位動畫、光源閃爍、製作名單;⬜ Hercules 尚非逐像素重現 |
| **遊戲機制** | 🔶 **子系統覆蓋高,逐函式核實 37%**(459/1232)。⚠ 兩個驗收門檻**從來沒跑過**:對 DOSBox 原版並排比對、game tester 正常玩家路徑(`CLAUDE.md §6.1`、`rulebook/65`)⇒ 「機制與原版一模一樣」目前**沒有任何外部證據**,只有靜態讀碼 |
| 中文化 | ✅ **文字本體實質完成**(2026-08-08 對程式碼核實):`.TLK` 對白 **1712/1713(99.9%)**、`SHOPPE.DAT` **194/194**、明文五檔 **114/114**(有測試強制)、`LOOK2.DAT` 216 段、`SIGNS.DAT` 招牌與墓碑、譯名層 304 筆、UI 字串。⬜ 剩:美術內嵌字母、**逐畫面巡查英文殘留**(P5 驗收條件,未跑) |

## 4. 執行計畫(2026-08-08 重擬)

### 4.0 為什麼重擬

前一版是「依檔案大小 + 逐個 OVL 補完」的清單,累積了上百行刪除線歷史,
而真正該回答的問題沒被排進去。這一版重擬的三個理由都核實過:

1. **進度指標是假的。** 舊版寫「機制約 99%」「中文化約 30%」——
   實際上中文化的文字本體是 **99.9%**(`.TLK` 1712/1713),
   而機制的逐函式核實是 **37%**。兩個數字都反了。
2. **清單變成日誌。** 最長一格 19,273 字元,五格超過 2,000 ⇒ 讀不完,
   所以同類問題每輪重新被發現。已改成 §5 的扁平清單。
3. ★★ **兩個驗收門檻從來沒跑過。** `CLAUDE.md §6.1` 與 `rulebook/65` 都明訂:
   驗收是**對 reference 實測比對**,不是自家測試綠。而整個專案至今
   **沒有跑過一次 DOSBox 原版並排比對,也沒有跑過一次 game tester**。
   ⇒ 「機制跟原版一模一樣」目前**沒有任何外部證據**。

第 3 點是根因。**靜態讀碼已經進入邊際收益遞減**(每輪找到的問題越來越細,
而剩下 763 支函式多數是繪圖與 I/O),但那不是換題目的理由 ——
是「這條路本來就驗不了目標」的理由。目標要的是行為一致,
而行為一致只能跟跑起來的原版比。

### 4.1 A 階段 —— ❌ **使用者決定不做**(2026-08-08),改用 IDA 深挖

> 使用者指示:「我認為不需要 A,因為我們有 ida_pro 可以深挖。」

**這個判斷大致成立,理由要寫清楚**:A 階段當初列的四條待裁決項目裡,
三條其實是**組語就能定案**的 ——「午夜前十分鐘不傳送」與「離開地牢放地表曲」
已經是逐指令讀出來的真值(`docs/re/86`、`87`),而「開局在城裡放哪一首」
是「`byte_3DDB0` 進不進存檔」的 xref 問題。**拿 DOSBox 去確認組語,方向是反的。**

⚠ **IDA 補不到的那一塊,誠實記著**:靜態讀碼證明「原版怎麼做」,
但證不了「我把它抄進 Go 時有沒有抄歪」,也證不了整條玩家路徑跑起來會不會
湧現別的問題(`CLAUDE.md §6.1` 的 soft-lock 形狀)。**這一塊改由 P7 的
`retro-game-playtest`(正常玩家路徑,不用 debug 旗標)承接**,不再獨立成一個階段。

⇒ 原 A1/A2/A4(DOSBox oracle 截圖與並排對照)取消;A3 併入 E 階段(見 §4.5)。
下表保留當紀錄,不再是待辦。

| # | 工作 | 驗收條件(決定性) |
|---|---|---|
| A1 | DOSBox 跑 DOS 原版,產一組固定 oracle 截圖:開場、建角、大地圖(不列顛城外)、城鎮內、Ztats、商店、戰鬥、地牢第一人稱。固定 `cycles`(**不用 `auto`**,可重現性的敵人)、固定存檔起點 | 同一組指令重跑兩次,截圖逐位元組相同 |
| A2 | 引擎 headless 用**同一個存檔、同一組座標**截同樣的畫面(`u5dump scene` 已具備),產並排對照頁 | 每組有一張並排圖;差異逐條記進 `docs/re/` |
| A3 | `retro-game-playtest` 的正常玩家路徑:**不用任何 debug 旗標**,建角 → 走到最近城鎮 → 對話 → 買賣 → 打一場 → 存檔 → 重開讀檔 | 全程不卡、不崩、存檔讀回一致;失敗項逐條開單 |
| A4 | 把 A1/A2 包成可重跑腳本 `tools/oracle.sh`,寫進 CI 的手動觸發 | 一行指令產出整組對照頁 |

⚠ 風險與對策:DOSBox 在 headless 要 xvfb,但**設 `output=surface` 就不需要 GL**
—— `docs/engineering-notes.md` 記的軟體 GL 死鎖是 ebiten 那條路,不是這條。
DOSBox 設定的實測基準見 `~/.claude/knowledge-base/retro/dosbox-game-configs.md`。

### 4.2 B 階段 —— 回合結構(十條,一起改)★ 現正施工

出處 `docs/re/81` §5,清單在 §5.1。**一起改一起測**,不然分不出哪條改壞了。
每一條都有位址,所以不必等 A 的對照就能動;A 之後再回來確認時鐘讀數與遭遇次數。

影響最大的兩條:

- **B1** 大地圖每回合都跑一個世界回合(`loc_2DD2F` 無條件)⇒ 現在**在草地上走路
  野怪不會動、不擲遭遇**。這是玩家一定會察覺的差異。
- **B2** 大地圖每回合 **2 分鐘**(引擎全模式 1)⇒ 大地圖時鐘走一半速度,
  連帶 NPC 排程、月相、日夜全都偏。

驗收:與原版並排走 100 步,時鐘讀數一致、遭遇次數同一個數量級。

### 4.3 C 階段 —— 音樂(唯一整個沒開工的階段)

`internal/audio` **不存在**。三個來源依成本排:

| # | 來源 | 做法 | 驗收 |
|---|---|---|---|
| C1 | FM Towns 兩條 CDDA | 從 `.chd`/`.cue` 直接轉 ogg | 兩軌可播,長度 180s / 371s |
| C2 | 場景 → 曲號 | ⚠ **不是「直接讀表」**(`docs/re/87` 更正):`.TBL` 只給**曲號 → 檔名**與六聲道音量,場景對應在**程式碼**裡 —— 32 個 `sub_3181C` 呼叫點 + `sub_31DC0` 的地點跳表。✅ **已逆完並落地**(`u5data.StartupSong`、曲號表、12 筆地點跳表)。⬜ 換場景時換曲要等 `internal/audio` | 曲號 → 檔名 15 列;開局選曲四條路都有測試 |
| C3 | 15 首 `.EUP`(EUPHONY) | u1-cht 逆過,可複用 | 每首轉出可播的 ogg |
| C4 | upgrade 的 19 首 `.XMI` | XMI→MID→fluidsynth+SoundFont,**離線** | 同上 |

⚠ C1/C2 就能讓遊戲有配樂。C3/C4 是完整度,不擋可玩。

### 4.4 D 階段 —— 長尾核實

- **D1** 截斷清單剩下的 19 支(§5.2)。前 91 支挖出五個沒接上的子系統;
  剩下這批多是報價字串與 Ztats 版面,但**照樣逐支讀完** —— 期望值低不等於零,
  而「沒讀」與「讀了沒缺口」是兩種結論(後者也要記,不然下一輪重查)。
- **D2** 單點未解(§5.3),十三條。**每一條都不准靠印象補**;語意不明就留白。
- **D3** `find_unwired.py` 剩下 22 個裡那六個「連測試都沒引用」的,逐一決定接或刪。

### 4.5 E 階段 —— 交付

| # | 工作 | 驗收 |
|---|---|---|
| E1 | P5 逐畫面巡查英文殘留 | 玩家可見文字 0 殘留;美術內嵌字母另計並誠實揭露 |
| E2 | P7 打包三平台 + `checkpkg.py` | 解壓即玩;**不打包測試存檔**(玩家走建角流程) |
| E3 | README 依 `rulebook/80` 三層 voice + 手冊引用(`CLAUDE.md §7.2` 五項) | 五項逐一滿足 |

### 4.6 執行順序(使用者指示 2026-08-08)

**B → C → D → E**。A ❌ 取消(見 §4.1),用 IDA 深挖取代。

- **B 階段 ✅ 已完成**(2026-08-08):回合結構十條 + 第 11 條的世界回合本體 +
  航行閘門 + 載入視窗 / 清場 / 敵船開砲(`docs/re/81`–`88`)。
- **C 階段施工中**:選曲邏輯與八個換曲點已落地(`docs/re/87`),
  剩 `internal/audio` 與 `.EUP` → 音訊。
- **逆向深挖不設限**:需要就往下讀,不必等症狀才動。
  截斷清單、單點未解、新主題都照做 —— D 階段不是「有症狀才讀」。
- 仍然守住的三條:
  1. 不補「原版沒有證據」的規則(`CLAUDE.md §3.0`,留白比猜著寫好)。
  2. 不為了讓稽核報告變綠而放寬檢查(已犯過一次:寬鬆中文關鍵詞會讓整行不檢查)。
  3. **下否定結論(「不必實作」「沒有這件事」「只有 N 處」)前做正對照** ——
     `docs/re/88` §2 的「參數從來沒被讀」就是靠對 `sub_2870` 的正對照才敢寫。
- ⚠ **靜態讀碼證不了的那一塊**:「原版怎麼做」與「我抄進 Go 有沒有抄歪」是兩件事。
  後者由 P7 的 `retro-game-playtest`(正常玩家路徑,**不用 debug 旗標**)承接;
  在它跑過之前,§3.1 那一列不得改寫成 ✅。

---

## 5. 未結案清單(扁平版,2026-08-08 起維護)

§2 的表格是「子系統盤點」,不適合當工作清單 —— 一格塞了幾千字之後就沒人讀得完
(2026-08-08 實測:最長那一格 **19,273 字元**)。所以未結案項目一律列在這裡,
**一條一行、附位址、附出處**,做完就刪。細節不要寫進本節,寫進 `docs/re/`。

### 5.1 回合結構 —— ✅ 十條已完成(2026-08-08),第 11 條另案

出處 `docs/re/81` §5。根因:`sub_0` 是三個互斥模式迴圈的切換器,而引擎用單一
`tick()` 服務所有模式。落地在 `internal/game/overworldturn.go`,
測試 `overworldturn_test.go`(11 條)。

| # | 項目 | 位址 | 狀態 |
|---|---|---|---|
| 1 | 大地圖每回合 **2 分鐘**(引擎全模式都用 1) | `sub_2D9D0` 的 `sub_29304(2)` | ✅ `minutesPerTurn()` |
| 2 | 每個大地圖回合都跑**一個世界回合**(無條件) | `loc_2DD2F: call sub_2E24` | ✅ 接上了,**但本體見第 11 條** |
| 3 | **幽冥界地震**:1/256 → 全隊 rand(1,8) 傷 | `sub_2D998` | ✅ `underworldEarthquake()` |
| 4 | **`(0xE9,0xEB)` 的「Pass, Seeker!」關卡** | `loc_2DC5F`、`byte_3E0DC` | ✅ `sacredQuestGate()` |
| 5 | 地形效果分流:`sub_1318` **只在場景**跑 | `sub_1318` ⇄ `sub_2D9D0` | ✅ `tick()` 依模式分流 |
| 6 | 大地圖**沒有**「睡著的人自己醒」(全檔掃描佐證) | `sub_1318` `loc_1323` | ✅ 含正對照測試 |
| 7 | 熔岩(tile 0x8F)在大地圖走另一支 | `sub_10BC4` | ✅ |
| 8 | 空白鍵(Pass)要走每回合收尾 | `sub_2ACF4` case 32 | ✅ `Pass()` → `tick()` |
| 9 | 地形代價的順序:**移動 + 代價 → 才是收尾** | `docs/re/81` §4 | ✅ `moveInWorld` 已調 |
| 10 | `sub_2B740(n)` = 重畫 n 次(純視覺) | `byte_3E0B4` | ⬜ 無阻塞動畫層,不做 |

★ 順帶結案 `docs/re/38` §5 的問號:**`byte_4FDD7` 是共用的** —— 全檔只有一個,
而 `sub_2E24` 的四個呼叫點都經過同一道閘門 ⇒ 騎馬 / 坐魔毯時**所有**世界回合
都隔次跳過(不是機率,是持久切換位元)。An Tym 全停、Rel Tym 隔次也一起做了。

### 5.1b 第 11 條:大地圖世界回合的本體 —— ✅ 生怪已完成,剩移動與漂流

接上 `extraWorldTurn()` 之後才發現它是**空殼** —— 只呼叫 `advanceNPCs()`,
而那支在非場景時第一行就 return。⇒ **大地圖上怪物不會動、不擲遭遇、
太遠的怪也不會被清掉**。原版 `sub_2E24` 過了閘門之後做四件事:

完整規格與已解出的部分見 **`docs/re/82`**。進度:

| 位址 | 做什麼 | 狀態 |
|---|---|---|
| `sub_1F98` | 遭遇門檻 | ✅ 實作(`u5data.EncounterThreshold`) |
| `sub_22B0` | 哪一槽算生物(依 **tile** 不是 `Kind`) | ✅ 實作(`u5data.IsCreatureTile`) |
| `sub_203C` + `sub_2008` | 依地形選怪種 + 加權抽樣;**四張表權重都恰好 256** | ✅ 實作(`u5data/spawn.go`) |
| `sub_2218` | 生怪流程(最多試 128 次落點) | ✅ 實作(`spawnWanderingCreature`) |
| `sub_215C` | 候選座標(兩軸離隊伍 7 格以上,環繞感知) | ✅ 實作 |
| `sub_2B57C` / `sub_2B518` / `sub_2B6C8` | 十趟優先序找槽 / 寫槽六個位元組 | ✅ 實作 |
| `sub_25F0` | 167 行 —— ★ **不是移動,是攻擊**(`docs/re/83` §1) | ✅ 相鄰 + 遠程已實作 |
| `sub_24DC` | 貼身接觸三條 —— ★★ **解開了 0xEC 是漩渦** | ✅ 實作(含小艇例外) |
| `sub_2D38` → `sub_2B24` | ★★ **怪物移動在這裡**(`docs/re/85`) | ✅ 追人 + 卡住退回遊走 |
| `sub_2A54` | 隨機遊走(最多試 3 次) | ✅ |
| `sub_2870` | 真的挪一格 —— ★ **地形會拖慢怪物**,會飛的四種免疫 | ✅ |
| `sub_2D2D0` 的節奏 | 揚帆時風向多燒 0/1/2 個世界回合 | ✅ `sailRhythm` |
| `dword_4FD50` 敵船速度表 | 值越大越快,`4` = 每回合都動 | ✅ `enemyShipSpeed` |
| 清場 | 離視窗超過 0x1F 格的**生物**清掉 | ✅ `cullDistantCreatures`(`docs/re/88` §1)。⚠ 只清生物 —— 少了這關,岸邊的船會憑空消失 |
| `byte_3E0AB` / `byte_3E0AC` | 載入視窗左上角 | ✅ `State.WindowX/Y`(`docs/re/88` §0):**對齊 16**,只在隊伍走進邊緣 5 格才**整塊捲 16**。生怪落點不再是近似 |
| `sub_23FC` 敵船開砲 | 正交同線且相隔 1..3,**無機率閘門** | ✅ `objectAttacks` + `turnBroadside`。⚠ 側身**只改 `ObjTile` 不動 `ObjKind`** ⇒ 不影響船速、不持久 |
| `sub_20CB4` 投射物動畫 | 回傳「有沒有被擋住」 | ⬜ 沒有動畫層 ⇒ **一律命中**(唯一的差異,已記) |
| `sub_27F0` + 11×11 遮罩表 | `sub_2D38` 的 `0xFC`(**影主**)那一支 | ✅ **確認不必實作**:`sub_2B24` 的第二參數**只被寫、從來沒被讀**(8 寫 0 讀,已做正對照)⇒ 兩條路做同一件事。⚠ 但 `inc 物件[槽].+5` 真的執行,而位移 5 是**耐久欄位** ⇒ ⬜ 影主的耐久會被慢慢加上去,副作用未追 |
| ~~怪物移動本身全檔找不到~~ | ✅ **已推翻**(`docs/re/85`) | 在 `sub_2D38` 的**最後一行** `call sub_2B24`。當初的結論是「讀了 `sub_2D38` 但沒讀到尾巴」——**「找不到」的成因是自己沒讀完**,不是原版沒有。⚠ 這也是「查詢回空要先做正對照」的一次(`diagnosis-notes/02`) |

### 5.1c ✅ `CanSail` 已用 IDA 深挖並改正(`docs/re/84`)

**閘門找到了:`sub_2CCFC` 的最後五行。** 玩家的航行只有一條規則 ——
**揚著帆(0x20..0x23)而且風是 Calm 時走不了;有風就照走,不管風向。**

風向真正的作用在 `sub_2D2D0`:揚帆時依「帆向 vs 風向」多燒 **0 / 1 / 2 個
世界回合**(垂直 0、同向 1、反向 2)⇒ 加的是**時間與怪物的行動機會**,
不是位移。✅ 已實作(`sailRhythm`,見 §5.1b)—— 此前這裡寫「還沒實作」是過期斷言。

★★ **順帶抓出風向表的三個抄錯**(五個獨立佐證交叉確認,見 `docs/re/84` §3):

| 錯 | 原本 | 正確 |
|---|---|---|
| 風值命名 | `WindWest=3` / `WindEast=4` | **`WindEast=3` / `WindWest=4`**(原版印的字串就是 East/West) |
| 南北向量 | 北風 → (0,−1) 往北推 | **北風 → (0,+1) 往南推**(名字 = 從哪來) |
| 船的朝向 | `ShipFacingW=1` / `ShipFacingE=3` | **`ShipFacingE=1` / `ShipFacingW=3`**(`sub_2CCFC` 跳表 0=北 1=東 2=南 3=西) |

⚠ 三個錯**互相掩護**:延遲表的對角線照樣對得起來,所以既有的自我一致性
檢查抓不到。要抓到得靠「原版自己印出來的名字」這個外部來源。
⇒ 教訓:**一張「值 → 方向」的表至少要兩個獨立來源才敢定案。**

⚠ `u5data.WindDelay.Delay` 現在沒有非測試呼叫者,而**那是有理由的**:
它是**敵船**的表,引擎還沒有敵船。已在原地寫明,免得下一輪被當成死碼刪掉
或被當成「已完成」。

### 5.1d(已被 §5.1c 取代,只留教訓)引擎的 `CanSail` 曾掛在錯的原版函式上

⚠ 下面這段的**結論已經被 §5.1c 推翻**(閘門在 `sub_2CCFC`,不在 `sub_2D2D0`;
座標更新在 `sub_2D014`;`sailRhythm` 已實作)。留著是因為「**先不改**」那個
判斷當時是對的 —— 半個理解換掉一個近似會更糟。原文:


`sub_2D38` 全檔**只有一個呼叫者**且讀的是物件槽 ⇒ 它是**敵船**的閘門,
與玩家的船無關。玩家的自動航行在 `sub_2D2D0`,用另外兩張表
(`byte_601C7`/`byte_601CB`)與另一條規則:`n = 1 + 不同的分量數`,
這回合走 `n % 3` 步 ⇒ **同向 1 步、反向 2 步、垂直 0 步**。

⚠ **本輪不改**:`sub_2D2D0` 的「走一步」那一段**沒有任何一行寫座標**,
所以我還不確定位置更新在哪裡(可能在 `sub_2D174`)。用半個理解換掉一個
至少「玩起來對」的近似會更糟 —— 方向對應搞反就會做出「順風走不動、
逆風飛快」的船。引擎裡兩處註解已就地標註掛錯,不讓下一輪照著錯的推理。

⇒ **這是 A 階段最省力的第一題**:在原版揚帆,數順風 / 逆風 / 側風各走幾格。

⚠⚠ **修正一個我自己的誇大**:上一個 commit 寫「玩家在大地圖上永遠不會被野怪襲擊」,
而 `sub_1F98` 顯示**原版的白天草地也不會**(門檻 1,而 `random(1,30) < 1` 永不成立)。

★★ 完整結論(`docs/re/82` §7):**白天的草地與海上在原版是安全的** ——
隨機遭遇只發生在崎嶇地形(沼澤/林/丘/山)、**凌晨 0–4 點**、或**幽冥界**。
這解釋了為什麼原版航海感覺平靜:不是遭遇表沒有海怪(有四種加敵船),
是門檻讓它們白天不會觸發。

實測(引擎 4000 回合):森林白天 3.4%(門檻 2 → 3.3%)、草地凌晨 9.4%
(門檻 4 → 10%)、幽冥界 6.5%(門檻 3 → 6.7%)、深水白天 **0**;
怪物分佈逐項對上表(Orc 24.1% vs 23.4%、Daemon 0.7% vs 0.4%)。

⚠ `docs/re/38` §4 的對應表把 `sub_2E24` 標成 `extraWorldTurn` 已實作 ——
過期斷言,已更正。

### 5.1e ✅ C 階段開工:配樂的選曲邏輯已逆完(`docs/re/87`)

**曲號 → 檔名定案**(15 列,兩個獨立來源:表的列數 vs `sub_3181C` 的上限 0x0E)。
⚠ 檔名編號不連續(`M92` / `M152` 夾在中間)⇒ 不能用「曲號 = 檔名數字 − 1」。

**開局選曲 `sub_31DC0` 定案並落地**(`u5data.StartupSong`):存檔有有效曲號就直接播 →
船 → 地牢 → 地點跳表(下標 16..27,剛好是 `CASTLE.DAT` 八個 + `KEEP.DAT` 前四個)→ default 1。

⚠⚠ **更正一條寫在 `CLAUDE.md` 裡的錯**:`U5_BGM.TBL` / `U5_SE.TBL`
**不是**「場景配樂對應表」,裡面沒有任何一欄是場景。錯因是只看表的形狀就推語意。

⚠⚠ **五條 `.SND` 測試從來沒跑過**:`snd_test.go` 自己那份 `fmtownsDir` 把
`U5_FMTOWNS` 當成 `U5_E` 目錄本身,而 `dev.sh` 與其他測試當它是 ISO 根 ⇒ 永遠 `Skip`,
而 **skip 不會讓 gate 變紅**。已統一慣例,五條現在真的跑且全綠。
⇒ 紀律:**看到 `--- SKIP` 先問「這台機器上明明有資料,為什麼跳過」。**

⚠ 差點把 `U5_SE.TBL` 的解析器重寫一遍 —— `snd.go` 早就有,而且處理了檔尾的
`0x1A`(DOS Ctrl-Z EOF),我那份沒有、跑起來會直接失敗。**先 grep 自己的程式碼。**

✅ **八個換曲點已落地**(`docs/re/87` §5):進場景 7、離場地表 1 / **幽冥界 0x0A**、
進地牢 9、離開地牢 1、月門 1、開戰 3、勝利 0、上船 / 上小艇 2。
★★ 「幽冥界另有一首」是從 `sub_86C` 的離場分支挖出來的
(`byte_3E0A3 == 19h` 印 "Underworld!" 配曲 0x0A,否則 "Britannia!" 配曲 1)。
⚠ `sub_177AC`(下載具)**刻意不接**:兩個呼叫點沒讀完,而 `dismount` 在場景裡
也會被呼叫(在城裡下馬)⇒ 接錯會在城裡放大地圖的曲子。有位址,沒讀完就不做。
⚠ `song` 刻意不匯出:曲號 **0 是勝利曲**,而 `State` 到處是結構常值 ⇒
匯出的話零值會被當成「正在播勝利曲」。`TestFreshStateHasNoSong` 釘住這個陷阱。

⬜ C 階段剩下的:`internal/audio`(不存在)、`.EUP` → 可播音訊
(`../u1-cht/tools/render_eup_music.py` 可複用,但那是自製 2-op FM 近似,
不是原版音色 —— EUP 檔頭 0x254..0x6D2 疑為音色定義,尚未解)、兩條 CDDA 轉檔、
換場景時換曲。

### 5.2 截斷清單剩下的 19 個函式

重算:`./tools/dev.sh python3 tools/hexrays_truncation.py re_work/fmtowns/WORRIORS.EXP.asm re_work/fmtowns/WORRIORS_hexrays.c --signal both --top 5000`

```
sub_12B20  sub_15DA0  sub_1E210  sub_203C   sub_20F24  sub_20F60  sub_24158
sub_253D4  sub_2557C  sub_25E4C  sub_265F0  sub_26718  sub_26D88  sub_26E08
sub_27BDC  sub_2B21C  sub_2C898  sub_30AB8  sub_B0DC
```

已知有機制重量的幾支:`sub_20F60`/`sub_20F24`(`That will be `/`gold for the `/`sir`/`milady`
—— 商店報價那一行,且 `0x20F24` 有死碼所以永遠取名冊 0)、`sub_1E210`(`Equipment` Ztats 頁)。

### 5.3 單點未解(語意不明,先不補假欄位)

| 項目 | 位址 | 為什麼還不能做 |
|---|---|---|
| `byte_3E0B2` 的 **0x10** 位元 | `sub_1EC34` 卸戒指時清它 | 時機知道、語意不知道 |
| `byte_3E09B >= 100` | 乞丐業報節流 | §2 曾記「進餐計數器」,**與此處用法對不上** ⇒ 兩處不一致本身是一條 finding |
| 物件種類 `& 0xFC == 0x6C` | 生物名表第 11 筆 | 疑為乞丐,未證 |
| `sub_1509C` | 與 `sub_15020` 成對的第二支獎品函式 | 未讀 |
| `sub_2AB38` | 寶箱陷阱傷害 | 引擎仍是 `random(1,20)` 估計值 |
| tile 0xAF | `It's open!` 但 `look#175` 叫「沉重的行李箱」 | 名稱與行為對不上,未解 |
| `dword_55A48` | 撬鎖進來設 1、失敗設 0 | 疑為地牢視圖重繪 |
| 寶箱的 0x08 位元 | 「頭上有洞」 | 兩支函式都只保留、從沒讀過 |
| `CombatModeTurns` ⇄ `TimeStop` | 共用 `byte_3E09E` | 引擎拆成兩個欄位 ⇒ 有分歧空間,待併 |
| 徽章欄位 | `0x0218` | 已定位,還沒接進 `u5data.Save` |
| `Absorbed!` 的三個未命名全域閘門 | `sub_1F840` 一帶 | 與 `docs/re/57` 的兩地點吸魔法**不同** |
| ~~埋下去的月石怎麼變成月門~~ | ✅ **結案**(`docs/re/86`) | **月門長在月石埋藏的座標上**:`sub_DEE4` 夜裡把 tile 0xDC 寫進 `byte_3E040/48[i]`(= 月石的 X/Y),白天寫回草地。`sub_E084` 讀的不是另一組表,是同一組 —— 差別在寫入與讀取兩步 |
| `find_unwired.py` 剩下 22 個 | — | 其中六個連測試都沒引用,「該接還是該刪」未逐一確認 |

### 5.4 本輪結案(留紀錄,避免重查)

- ✅ **`sub_2A50C` 不會一回合跑兩次** —— 四個呼叫點分屬四個互斥迴圈(`docs/re/81` §2)。
  中毒每回合掉 1 血,不必改碼。附帶結論:**戰鬥中不算維生開銷**。
- ✅ **`docs/re/74` §1「踏進沼澤擲兩次」已推翻** —— 兩顆骰子區分的是**地點**
  (大地圖 1..30 / 場景 0..29),同一回合只擲一次(`docs/re/81` §3)。
- ✅ **`docs/re/38` §4 的順序斷言已更正**(`docs/re/81` §4)。
- ✅ **文件斷言機械稽核上線**:`tools/check_doc_claims.py`。首跑 36 筆,
  真的錯的 5 筆已全部修掉(兩個不存在的 Go 符號、一個 16-bit 旗標被寫成 8-bit、
  `CONTEXT.md` 與 `PLAN.md` 各一條「`TILES.16` 壓縮未破」)。
  ⚠ 其餘 32 筆是誤報,原因是**資料位址常由 `base + offset` 算出、IDA 不會命名**。
  第一版用「白名單 + 放寬中文關鍵詞」壓掉誤報 —— 那讓報告變綠但**整行不檢查**,
  已改成**只查 `sub_` 函式**(有 xref 就必然有符號,零誤報空間),
  推導算式併回 `docs/re/24` 與 `docs/re/10` 兩份會被讀到的筆記裡。
- ✅ **函式索引補了 366 個符號**(551 → 917)。⚠ `gen_func_index.py` **印到 stdout
  不自己寫檔**,這一輪就因為把輸出丟進 `/dev/null` 而白跑一次。
- ✅ **月門真的寫進地圖緩衝了**(`docs/re/86` §4–5):`RefreshMoongateTiles`
  夜裡把 tile 0xDC 寫進月石埋藏的座標、白天計數器歸零後寫回草地,
  而 `render` 的地圖窗直接讀 `TileAt` ⇒ **月門畫得出來了**。
  同時把「座標 + 時段」那條**第二個真相來源**拆掉(`MoongateAt` 已刪),
  `EnterMoongateHere` 改成只讀 tile —— 與原版 `sub_E084` 一致。
  ⚠ 三個可觀察的原版行為照原樣留著:關門寫死草地(會蓋掉沙漠 / 雪地)、
  離開視窗不寫回、淡出節奏改成每回合(原版是每次重畫,唯一的近似)。
- ✅ **`sub_E084` 整支讀完,補上兩個機制 + 動畫格繪圖**(`docs/re/86` §6–7):
  - **踏過的月門立刻變回草地**(判傳送**之前**就寫 tile 5),夜裡下一次重寫回門
    ⇒ 「吸走你之後閉合、再張開」。
  - ★★ **午夜 00:00–00:09 踏上月門不會傳送** —— 門照樣關掉、人留在原地,
    **沒有訊息也沒有動畫**。照原樣做,不補提示。
  - ★★★ **`byte_3E097` 的繪圖那一半找到了**:全檔掃描發現**五個**使用者
    (原本只認得「開關 + 存檔」),缺的是 `sub_29BEC` —— 計數器 1..15 時改叫
    `sub_265F0` 畫中間格,**滿格才是完整的門**;而**計數器 0 走一般畫法**
    正好對上「離開視窗的殘留會被畫成完整的門」。`sub_265F0` 的算式
    (草地 + 門圖前 f 列疊在第 16−f 列)已落地成 `drawMoongateFrame`
    ⇒ **淡出動畫看得見了**,`MoongateFrame` 不再只是開關。
  ⚠ 又一次「`offset sub_1B800` 是 IDA 自動命名,不是函式」——
  0x1B800 ÷ 0x200 = 0xDC,它是 **tile 位移**。本專案第四次踩這個形狀。
  ⬜ `sub_135FC`(用同一個計數器的某段過場:長出月門 → 清空全部物件槽 → 收回)未讀,留 D 階段。
- ✅ **`0xEC` 的語意結案:它是漩渦,不是怪物**(`docs/re/83`)。`DATA.OVL` 名表
  索引 43 空白是**正確的** —— 它不在生物命名空間裡。三重佐證:組語字串
  `"WHIRLPOOL!"`、物件種類碼 **+256** 後 `look#492` =「漩渦」、以及庫裡早就
  翻好的 NPC 傳聞(`DWELLING.TLK#2#e11`)。
  ⚠ 我先前拿 `look#236` 去對是錯的:**物件種類碼與地圖 tile 是兩個命名空間**,
  種類碼要先 +256 才能查 tile 名。
- ✅ **回合結構十條落地**(§5.1)。過程中刪掉兩支死碼:`RoughSeasHere()`
  (`overworldTurnEnd` 用單次讀到的 tile 直接判,再讀一次會偏離原版)、
  `anyCreatureOnTheMap()`(為一個測不到的斷言而寫)。
  ⚠ 又踩到一次「**紅燈的成因在前置條件而不是結論**」:順序測試用山(tile 12)
  當 2 級地形,而山**擋步行** ⇒ `Move` 直接受阻、時鐘一分不動,紅得像順序錯了。
  換成丘陵(11)。這個形狀本專案已踩第六次,測試裡留了註解。
